import { createStyleSheet } from '../utils/dom';
import './common.css';

/**
 * Initializes all styles needed for the application
 */
export const initStyles = () => {
  // Additional dynamic styles can be added here using createStyleSheet
  // This ensures that common.css is loaded via webpack's CSS loader
}; 